# Mizo Text Training Site v2
Workflow: English + AI Mizo → C (Correct) OR correction → Save → Next.
Voice is not required now. The saved data model keeps a `voice: null` field so voice recording can be added later without changing sentence IDs.
Included recoverable dataset from the visible chat context: Short #331–403 and Long #1–18.
The earlier Short #1–330 should be imported once the exact source data is available.
Run: npm install && npm run dev
